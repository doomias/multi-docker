language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t doomias/react-test -f ./client/Dockerfile.dev ./client

script: 
    - docker run -e CI=true doomias/react-test npm test

after_success:
    - docker build -t doomias/mutli-client ./client
    - docker built -t doomias/multi-nginx ./nginx
    - docker build -t doomias/multi-server ./server
    - docker build -t doomias/multi-worker ./
    #Log in to the docker CLI
    - echo "$DOCKER_PASS" | docker login -u "$DOCKER_ID" --password-stdin
    #Take those images and push them to docker hub
    - docker push doomias/mutli-client
    - docker push doomias/mutli-nginx
    - docker push doomias/mutli-server
    - docker push doomias/mutli-worker
    